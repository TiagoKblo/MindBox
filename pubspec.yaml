name: livro
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  sqflite: ^2.3.3+1 # Ajustado para a versão mais recente
  path_provider: ^2.0.1
  url_launcher: ^6.1.7
  cupertino_icons: ^1.0.8
  path: ^1.9.0 # Mantido de acordo com a versão mais recente

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  assets:
    - assets/images/logo4.jpeg

  uses-material-design: true
